# Basic-Billing-System
This system is a basic prototype using c++ of our shopping billing feature.
Hope you like it.
# This billing system complies over wsl(Windows subsytem for Linux)
# If you want to use it for windows uncomment the <windows.h> header file and change the sleep() attribute to Sleep() as well the system("clear") to system("cls").

